// Problem 4
const objectProperties = (obj) => {
  console.log(`
  Name: ${obj.name}
  Age: ${obj.age}
  City: ${obj.city}
  `)
};

person = {
  name: 'Mark',
  age: 25,
  city: 'London',
};

objectProperties(person);